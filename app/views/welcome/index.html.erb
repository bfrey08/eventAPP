<!DOCTYPE html>
<html>
    <head>
        <title>this is your event bitch </title>
        <meta charset="utf-8">
    </head>
    <body>
        <section id="eventfront">
            <div id="wrapper">
              <% if @attendee %>
                <%= link_to("find your events", "/#{@attendee_id}") %>
              <% else  %>
                <%= link_to("find your events", "/verify") %>
              <% end %>
            </div>
            <div>
              <%= link_to("make a new event", "/events/new") %>
            </div>
        </section>
    </body>
</html>
